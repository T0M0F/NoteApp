import 'package:boostnote_mobile/presentation/pages/notes/NotesPage.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
/*
  testWidgets('Note should open on click', (WidgetTester tester) async {
    await tester.pumpWidget(NotesPage());
    final listTileFinder = find.byType(GestureDetector);
    listTileFinder.

  });
*/
}